{% extends 'base.html.twig' %}

{% block title %}Autor{% endblock %}

{% block body %}
    <h1>Autor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>AutId</th>
                <td>{{ autor.autId }}</td>
            </tr>
            <tr>
                <th>AutNombre</th>
                <td>{{ autor.autNombre }}</td>
            </tr>
            <tr>
                <th>AutApellido</th>
                <td>{{ autor.autApellido }}</td>
            </tr>
            <tr>
                <th>AutFechaNac</th>
                <td>{{ autor.autFechaNac ? autor.autFechaNac|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autor_index') }}">back to list</a>

    <a href="{{ path('autor_edit', {'autId': autor.autId}) }}">edit</a>

    {{ include('autor/_delete_form.html.twig') }}
{% endblock %}
